Unordered array is most preferable if we have huge no of insert operations because Unordered array have order of insertion as 1 in worst case. and removing has O(N). while ordered array has large complexities while adding.